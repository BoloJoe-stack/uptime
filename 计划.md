# uptime · 项目计划

（原名"摸鱼控制台"，2026-09-02 更名 uptime——对外伪装成监控工具）

> 一句话：Python 写的上班摸鱼全家桶。终端为主战场，托盘常驻，热键切换。策略=先做单件（各自能独立跑），最后整合成控制台壳。
> 命名原则：一切对外可见的名字（文件夹/窗口标题/任务栏/进程/git 仓库）一律用工作味代号，"摸鱼"相关字样不出现在任何对外位置。

## 一、模块清单（7 件）

| # | 模块 | 一句话定义 | 难度 |
|---|------|-----------|------|
| 1 | 实时工资跳动器 | 按月薪算出每秒单价，终端数字每秒跳动，实时显示"今日已赚" | ⭐ |
| 2 | 节假日/下班倒计时 | 距下班精确到秒 + 下一个法定假日倒计时，调休补班识别 | ⭐⭐ |
| 3 | 假代码滚动器 | 终端自动滚打印像样的代码+日志，穿插 [INFO]/[WARN]，偶尔卡住像在 debug | ⭐⭐ |
| 4 | ASCII 鱼缸 | ~~终端字符鱼游动+气泡+水草~~ **已删除（2026-09-03）**：用户拍板（代码在 git 历史） | ⭐⭐ |
| 5 | 贪吃蛇 | ~~字符渲染可玩，外层套"日志查看器"伪装皮肤，热键露出真容~~ **已删除（2026-09-03）**：改纯鼠标后皮肤切换键不可达，观感与 tail 重复，用户拍板删除（代码在 git 历史） | ⭐⭐⭐ |
| 6 | 老板键 | 全局热键一键隐藏全部摸鱼窗口+前置工作窗口，<100ms | ⭐⭐⭐ |
| 7 | 摸鱼控制台（壳） | 系统托盘常驻，菜单+热键调度各模块，统一配置 | ⭐⭐⭐ |
| 8 | 可视化主面板 | 游戏目录风卡片启动器+全设置，纯鼠标操作（2026-09-03 立项） | ⭐⭐⭐ |

## 一·补 · 命名伪装表（对外一律用代号）

| 模块 | 代号 | 伪装逻辑 |
|---|---|---|
| 工资跳动器 | `burn` | burn-rate 烧钱率，云成本监控既视感 |
| 倒计时 | `eta` | 构建/部署 ETA，最无聊最安全 |
| 假代码滚动器 | `tail` | tail -f build.log 的样子 |
| ASCII 鱼缸 | `boids` | ~~群体模拟算法名~~ 已删除 |
| 贪吃蛇 | `less` | ~~终端 pager 名，暗讽 less is more~~ 已删除 |
| 老板键 | `focus` | "专注模式"，字面工作词汇 |
| 控制台壳 | `uptime`（主进程） | 监控面板，托盘 tooltip 写 "uptime service" |
| 主面板 | `panel` | dashboard/面板，工作监控台既视感 |

- 窗口标题统一格式：`uptime — <代号>`（如 `uptime — burn`）
- 代码目录、模块名、进程名一律用代号；中文全名只出现在本计划文档里
- 各模块渲染界面里的文案也去生活化：货币符号、具体数字的可读性在阶段 1 细化

## 二、阶段计划（先易后难：速胜 → 演技 → 娱乐 → 防御 → 整合）

### 阶段 0 · 骨架 ✅（2026-09-02 完成，commit 03a7f43）
- 建目录结构（见第四节）+ `requirements.txt` + `config.json`
- config 内容：月薪、上班/下班时间、工作日定义、各热键键位、摸鱼窗口标题黑名单、工作窗口标题
- 公共渲染小工具（rich 封装：全屏 Live、主题色）

### 阶段 1 · 速胜组：工资跳动器 + 倒计时 ✅（2026-09-02 完成，burn=d6ed7ad / eta=f521d87；工作流 7 代理，两模块均 QC 一次过+验收通过）
- 工资跳动器：读 config 月薪 → 每秒单价 → 今日已赚实时跳 + 今日进度条
- 倒计时：工作日显示距下班时分秒；节假日数据先联网查 2026 全年放假安排+调休，生成 `data/holidays.json`（每年手动更新一次）
- 验收：两个都能独立跑，数字刷新无闪烁（rich Live）
- 验收遗留（非阻塞，不急着修）：进度条格子按 round 取整与百分比文字略偏；非 TTY（管道/重定向）模式只输出单帧静态面板、AUTO_EXIT 不计时（rich 固有行为，真实终端一切正常）

### 阶段 2 · 演技组：假代码滚动器 ✅（2026-09-02 完成，QC一次过+验收通过）
- 预生成本地语料（多语言风格轮播：Python/JS/Go），不联网
- 效果细节：深色主题快速滚动，穿插日志行，随机"卡住 3-5 秒"更像真在 debug
- 验收：连放 10 分钟不出戏、不重复穿帮
- 实测（验收代理数据）：60 秒 623 行全唯一、连续 5 行窗口零重复；卡顿开关 A/B 行数 -38.2%；卡顿时长 3~5 秒精确；出戏度判定"像"（多语言代码地道，进度数字单调）。小瑕疵：file:line 前缀更像 grep dump 而非编译器输出，与"调试 dump"人设自洽，不阻塞

### 阶段 3 · 娱乐组：鱼缸 + 贪吃蛇 ✅（2026-09-02 完成，boids=QC一次过+验收过 / less=QC一次过+验收打回1轮返工后复验过）
- 鱼缸：rich Live 动画，鱼随机游动转向，气泡上升；验收=CPU 占用 <5%
- 贪吃蛇：msvcrt 非阻塞读键（Windows 原生，不依赖 keyboard），记分+暂停；伪装皮肤=默认滚日志，热键切换真容，Esc 切回
- 验收：贪吃蛇能玩能死能重开，皮肤切换 <1 屏闪动
- 实测（验收代理数据）：boids CPU 累计 ~2.4%、真终端 11.9fps、9 组尺寸突变 torture 3000 步零出界；less SEED=7 全对局 81/81 帧逐字节可复现、反向输入正确忽略、board 期间日志照滚
- 测试基建坑（未来阶段复用）：winpty 0.4.3 在无头会话因 pty 尺寸 0 断言失败，验收代理自建 ctypes CREATE_NEW_CONSOLE + WriteConsoleInput 注入 harness 替代，覆盖面不弱于 winpty

### 阶段 4 · 防御组：老板键 ✅（2026-09-02 完成——历经限额中断+代理失速自动重试，09-03 收尾，QC一次过+验收过）
- keyboard 注册全局热键；pywin32 枚举窗口，按标题黑名单最小化摸鱼窗口，SetForegroundWindow 前置工作窗口
- 已知坑：SetForegroundWindow 有系统限制（需 AttachThreadInput 技巧）；再来一次热键=恢复（老板走了按回来）
- 验收：从按键到全部窗口处理完 <100ms，连按 20 次不失效
- 实测（验收代理数据）：处理耗时 1~6ms（余量 16~100 倍），真机最小化生效 52~123ms，最大化状态还原保真；9 个真实桌面窗口全程零误伤；40+ 次热键注入零崩溃；损坏状态文件/窗口被手关/非法热键等边界全优雅
- 环境备注：锁屏/无头会话下合成输入会被系统吞掉，"干净 20 连击注入"无法在该环境完成——对照实验证明系环境限制，等效 20 次 ONCE 交替 20/20 通过；真机使用不受影响

### 阶段 5 · 整合：控制台壳 ✅（2026-09-03 完成，QC一次过+验收通过；工作流历经中转站分类器宕机中断一次，换模型后存档脚本续跑）
- pystray 托盘常驻（Pillow 程序化图标：深色圆角底+绿色脉冲波，缓存 %TEMP% 可自愈），托盘菜单启动/前置各模块，tooltip "uptime service"
- 统一热键表 ctrl+alt+1~6（config 可改，键位冲突逐条告警跳过不崩）；单实例 msvcrt 锁（%TEMP% pid 文件，崩溃自动释放）；启动自动最小化自身；菜单/热键/LAUNCH 三入口统一 dispatch：未运行→cmd 新控制台启动（防闪退），已运行→前置（AttachThreadInput 手法同 focus）
- 实测（验收代理数据）：构建自测 10 脚本 119 项全过；DUMP 结构自检 rc=0；LAUNCH=burn 真窗口 2.5s 出现 5s 自灭零残留；已运行二次触发走前置（同 hwnd/pid、进程数不增）；单实例第二实例 rc=2 提示 pid、kill -9 锁残留后新实例正常接管；干净退出多轮 rc=0 且不牵连已启动子模块（壳退了 less 继续活）；30 秒真跑自动最小化+经 LAUNCH 拉起 eta/boids 真窗口；console 段缺失/hotkeys 空/非法值全优雅
- 遗留（非阻塞）：托盘图标真实鼠标点击、show_state ●/○ 菜单刷新视觉、真实键位热键手感——无头环境无法验证，留人工验收；热键行为以直调回调等效覆盖（锁屏环境合成输入被吞，阶段4同款结论）
- 可选加分项：pyinstaller 打包 exe ✅（见下"打包交付"）/ 开机自启（未做，待定）

### 打包交付（2026-09-03，阶段5收官后）✅

- `py -3.10 build_exe.py` → 单文件 `dist/uptime.exe`（约 26MB，已复制到桌面）
- 入口多路复用 `uptime/__main__.py`：不带参数=托盘壳；`uptime.exe burn` = 直跑单模块（托盘壳拉子窗口同款命令，源码模式 `py -3.10 -m uptime` 等价可用）
- frozen 适配：config.json 首跑在 exe 旁自动生成（改月薪就在桌面那个 config.json）；data（节假日/语料）与配置模板打入 exe
- 打包测试：沙箱独立目录跑 DUMP 结构自检 rc=0；六模块全部经真控制台窗口拉起、4~6 秒自灭、壳 rc=0、零残留进程；单实例锁 exe 形态生效；无 Git Bash 管道直跑时的 GBK 报错为 rich legacy 渲染伪影（真窗口路径无此问题，与阶段4验收同结论）
- 开机自启：未做（待定）

### 阶段 6 · 面板：可视化主面板（panel）✅（2026-09-03 交付，**轻量模式首单**：构建自测12组全过 + 主会话冒烟即发，未跑三重复验；feat 2196942）

背景与决策（2026-09-03 用户实测反馈）：托盘图标藏在 Win11 `^` 折叠区看不到；全局热键不好按且无头环境无法真机验证；关掉控制台窗口=误杀壳。三项决策：**双击 exe 直接弹面板窗口**；**纯鼠标操作，不注册任何热键**（config 默认 hotkeys:{}）；**不做开机自启**。

- tkinter 深色主窗口 `uptime - panel`（游戏目录风卡片网格），exe 启动即显示；控制台黑窗 FreeConsole 消失；面板 X=隐藏到托盘（进程不死，修复误杀坑）
- 五模块卡片（burn/eta/tail/boids/less）：点卡=未运行启动/已运行前置；运行中高亮+结束按钮；状态每秒刷新
- 隐身按钮：一键收面板+全部 "uptime - " 窗口，再点恢复（复用 focus 现成窗口逻辑，不碰键盘钩子）
- 设置区：月薪/上下班时间/午休/每周工作日/show_state；保存写当前生效 config.json（下次启动模块生效）
- 托盘菜单改为：模块×5 + Panel + exit；tooltip 不变；单实例锁保留
- 交付流程：轻量模式（构建代理全场景自测 12 组全过 → 主会话冒烟：双 DUMP 钩子/真跑弹窗/零残留）→ 重打包 exe 已换桌面（29.8MB，含 tkinter）；设计规范来自 ui-ux-pro-max（Dark OLED + code dark/run green 色板），QC 视觉检查项由构建自测的视觉断言覆盖

### 阶段 7 · 挂件化：burn/eta 桌面小挂件 + 删鱼缸 ✅（2026-09-03 交付，轻量模式：我直接写+冒烟，UI 方案 grill-me 访谈+ui-ux-pro-max 选型）

- **架构**：burn/eta 改为与面板同进程的 Tk Toplevel 挂件（`uptime/widget/` 包：WidgetBase 底座+两皮肤），点卡秒开、不走子进程——PyInstaller 父校验那类坑对挂件绝迹；tail 保持终端子进程形态
- **burn 挂件（美式动漫风/Neubrutalism）**：黄底#FFEB3B+粗黑边+波点+硬投影+大红粗字；今日/本月金额默认打码、悬停显真值（薪资保密）；每秒金币动画（+秒薪 爆炸框上飘，逢10秒 KA-CHING!）
- **eta 挂件（赛博 HUD 风）**：纯黑+荧光绿+四角取景框+扫描线+闪烁光标；距下班大倒计时+今日进度+下个假期（HOL:名 天数d）
- 公共底座：无边框置顶（config widgets.always_on_top）、整窗拖动、位置记忆（widgets.<code>_pos，松手写盘）、×关闭、悬停事件；标题保持 "uptime - <代号>"，隐身按钮天然覆盖
- boids（鱼缸）删除（commit 可找回）；面板卡片 3 张（burn/eta/tail）；托盘菜单 3 模块；`uptime.exe burn/eta` 单跑=直接开挂件
- 踩坑记录：① 无边框窗在 mainloop 未跑/未映射时设坐标会被 WM 覆盖→绑 `<Map>` 一次性落位+update_idletasks 同步；② 面板传 console 段而非全量 config→挂件自行补载完整配置；③ 单跑模式的 withdraw 主根也带窗口标题，枚举时会出现"幽灵窗口"（无害，属同进程）
- 视觉验收：截图+图像分析两轮（爆框字距/进度条对比/暗字对比三处微调后通过）；拖动/悬停/动画手感留用户实机验收

### 补丁 · 配置迁移 %APPDATA%（2026-09-03 轻量：用户拍板桌面不留明文 config）

- 打包形态真实配置改存 `%APPDATA%\uptime\config.json`（`common/config.py` 的 `_user_config_path`）；首跑缺失自动生成默认；exe 旁若有老版本遗留 config.json 且用户目录为空 → 自动迁移并删旧文件
- 桌面从此只有 uptime.exe；面板保存设置、挂件位置记忆都走 `config_path()` 同口径，自动跟随；源码形态不变（仓库根 config.json）
- 验证：临时 APPDATA 模拟 frozen 三场景（首跑生成/遗留迁移/save 写回）全过 + 真机 exe 冒烟

### 补丁 · burn 进度条改发薪周期（2026-09-03 轻量：用户口头需求）

- burn 挂件进度条口径：**今日工作进度 → 发薪周期**。进度 = 距上次发薪已过时长 / 整个周期，**发薪瞬间归 0、整月线性爬向下次发薪满格**（用户拍板线性、不要放缓曲线）；发薪 = **每月 10 号 18:00**，当天非工作日（周末/法定假日）**前移**到最近的前一个工作日（仍 18:00 发；调休补班日视为工作日不挪）
- 实现：`uptime/burn/__init__.py` 新增纯函数 `payday_of()` / `payday_cycle()`（复用 eta `is_workday` 口径，holidays.json 一致），`progress` 字段语义未动（eta 挂件/终端面板不受影响）；挂件新增底部「距发薪 X天 HH:MM:SS」倒计时行（**精确到秒**），今日/本月金额、金币动画保持原样
- 测试钩子：挂件 `_update` 改用 `get_now()`（`UPTIME_FAKE_NOW` 可注入截图）；回归脚本 `build/_payday_smoke.py`（gitignored，不入库）

### 补丁 · burn 双小动画：金币（每秒）＋美钞（跨整元）（2026-09-03 轻量）

- 删掉原每秒飘的「+秒薪」数字动画，改为**两处小动画**（用户拍板：金币+美钞两处、美钞只在金额进位时抛，避免每秒双动太闹）：
  - **金币**：上班中每秒在金额右侧（约 x210,y88）抛一枚**泰拉瑞亚风像素金币**——以官方 Gold Coin 贴图（terraria.wiki.gg，12×16，透明底 7 色）逐像素复刻成代码贴图，COIN_SCALE=1（用户嫌大从 2 缩到 1 倍原尺寸，线性减半）画出；用 PIL.ImageTk 整张贴成**单图片项 + 预生成 8 张淡出帧**，飘起即淡没（不要逐方块变色——实测太重，一条动画被拖到 ~2.5s）。无数字
  - **修**：挂件 H 150→160——底部「距发薪」行字体行框高 19px，H=150 时下缘被卡片底(145)截断只露上半
  - **美钞**：今日已赚金额每跨一个整元（如 12.34→13.00，约每 90 秒一次）时，从金额数字右上方抛 6 张小美钞（绿/亮绿/金褐混合小方块，上抛抛物线淡没）。进位判定按**显示金额**（四舍五入到分后的整元，`round(元×100)//100`），与画面个位数 +1 **同帧**弹——不用 `int(元)` 截断，否则会落后画面约 1 秒（踩过，已修）
- 触发只认上班中（`state=="during"`，口径与旧金币动画一致）；下班/周末晚 18:00 后不弹。金额打码与否都照弹（纯装饰、无数字泄漏）
- 实现：`widget/burn.py` `_coin_pop()` / `_bill_burst()`（共用 `_mix_hex` 淡出），`_update` 用 `_last_floor` 检测整元进位；回归脚本 `build/_burn_anim_smoke.py`（金币300/美钞3/下班停/自清理全过，gitignored）

### 补丁 · eta 下个节日倒计时精确到秒（2026-09-03 轻量；进度表功能用户已取消）

- eta 挂件底行 `HOL:<名>` 从「还有 X 天」改为**精确到秒**：`HOL:中秋节 21d 12:00:00`，不足 1 天显示 `HOL:中秋节 15:00:00`（目标=节日首日 0 点，与原来 X 天同一个点，仅补秒）；正处节中仍显示 `HOL: now <名>`
- 挂件时间改走 `get_now()`（与 burn 一致，`UPTIME_FAKE_NOW` 可注入）；纯终端 rich 面板未动

## 三、技术栈

- Python：`py -3.10`（本机约定）
- `rich`：终端渲染（1/2/3/4/5 全靠它）
- `keyboard`：全局热键（老板键+贪吃蛇皮肤切换）
- `pywin32`：窗口枚举/操作（老板键）
- `pystray` + `Pillow`：托盘+图标（阶段 5）
- `msvcrt`：贪吃蛇键盘输入（标准库）
- 可选：`pyinstaller`（打包）

## 四、目录结构（规划，开工时建）

```
uptime\
├── 计划.md
├── requirements.txt
├── config.json              # 月薪/上下班时间/热键/窗口黑名单（.gitignore 排除，不入库）
├── config.example.json      # 配置模板（入库）
├── data\
│   ├── holidays.json        # 2026 放假+调休（每年手更）
│   └── code_corpus\         # 假代码语料
└── uptime\                  # 包名 = 项目名
    ├── common\              # 配置读取、渲染工具、热键注册
    ├── burn\                # ① 工资跳动器
    ├── eta\                 # ② 倒计时
    ├── tail\                # ③ 假代码滚动
    ├── boids\               # ④ 鱼缸
    ├── less\                # ⑤ 贪吃蛇
    ├── focus\               # ⑥ 老板键
    └── console\             # ⑦ 控制台壳
```

## 五、坑位预警

1. **调休数据**：国务院安排每年 11-12 月才发布次年，holidays.json 每年手更新（倒计时启动时检测年份并提醒）
2. **pyautogui failsafe**：鼠标相关操作若用到，注意角落急停机制（本项目老板键不依赖它，用 pywin32 更稳）
3. **SetForegroundWindow 限制**：Windows 不允许后台进程抢焦点，需 AttachThreadInput 或模拟 alt 键（阶段 4 处理）
4. **终端闪烁**：所有动态模块必须用 rich Live 整帧刷新，禁止逐行 print
5. **键盘钩子冲突**：keyboard 库是全局钩子，选热键避开飞书/IDE 常用组合

## 六、待定决策

- [x] **git**：✅ 已定（2026-09-02）——单库 monorepo，仓库/文件夹名 `uptime`；.gitignore 排除 config.json（月薪隐私不入库），入库 config.example.json 模板；身份 Bolo <Bolo_Joe@163.com>；commit 信息用英文工作味措辞，配合伪装
- [x] **打包 exe**：✅ 已交付（09-03，单文件 exe 在桌面，阶段7版含挂件）；**开机自启**：❌ 不做（09-03 用户拍板）
- [x] **月薪等隐私**：✅ 已定（09-03）——config.json 手填、代码不写死、不入库；本机两份 config 均已填好真实值，待办清零
