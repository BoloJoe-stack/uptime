# 更新日志

本文件记录 uptime 每个发布版本的内容。所有**正式版本**都会在 [Releases](https://github.com/BoloJoe-stack/uptime/releases) 同步发布，附可下载的 `uptime.exe`；改动列表以此处为准。

版本格式：`v主.次.修订`。每发布一版，在本文件**顶部**新增一段。

---

## [v1.1.0] - 2026-09-04

**更新自检**。托盘壳启动时后台检查更新，发现新版本会托盘通知；菜单新增两个入口。

### 新增

- **启动自动检查更新**：uptime（托盘壳）启动后静默查一次最新版，有新版时托盘气泡提示；无新版/网络不可达静默不打扰
- **托盘菜单两项**：`check update` 手动检查（无新版提示已是最新）；有新版时出现 `download update`，点它直达下载页
- **更新源可配置**：`config.json` 新增 `updater` 段——`enabled` 开关、`url` 指向任意"返回兼容 JSON 的更新源"（默认空 = GitHub Releases；国内无代理连不上时静默；想换源填一个可达地址即可，见 README §3/§7）

### 修复

- 内置版本号自立项起停在 `0.1.0` 未跟随发布，现已与 Release 版本号同步（`uptime/__init__.py` 的 `__version__`）

---

## [v1.0.0] - 2026-09-04

**首发版本**。桌面时间 / 收入可视化小部件合集（Windows）。

### 挂件

- **burn（烧钱率卡片，美元纸币风）**
  - 今日 / 本月已赚：按月薪、上下班时间、午休、工作日实时折算；默认**打码**，鼠标悬停显示真值
  - **发薪周期进度条**：每月 10 号 18:00 发薪（遇周末/法定假日自动前移到最近的前一个工作日，补班日不挪）；发薪瞬间归 0，整月线性爬向满格
  - **距发薪倒计时**：底部 `X天 HH:MM:SS`，精确到秒、贴整秒跳动
  - 上班时段小动画：每秒一枚泰拉瑞亚风像素金币、每 5 秒一小把美钞，弹起淡出、纯装饰
- **eta（倒计时卡片，赛博 HUD 风）**
  - 距下班大倒计时，精确到秒、整秒对齐
  - 今日进度条（上班时间进度）
  - **下一节假日倒计时** `HOL:<名>`：终点 = 放假前最后一个工作日的下班时刻（18:00），正处假期显示 `HOL: now <名>`

### 外围

- **console**：系统托盘壳，菜单调度各模块，常驻
- **panel**：可视化面板，一键开/收全部挂件，设置区失焦即时校验
- **tail**：日志流终端（占位/演示）
- **focus**：一键收起全部挂件窗口（热键/面板按钮）

### 配置

- `config.json` 首次运行**自动生成**（gitignored，不入库）——拿源码零配置可跑
- 打包 exe 形态配置存 `%APPDATA%\uptime\config.json`，exe 旁/桌面**不留明文配置**；换 exe 不丢设置
- 可配置项：月薪、月工作日、上下班时间、午休分钟、每周工作天数、节假日数据、挂件位置记忆（拖动自动保存）等

### 运行 / 打包

- 源码运行：`py -3.10 -m uptime`（需 Python 3.10）
- 单文件 exe：`py -3.10 build_exe.py` → `dist/uptime.exe`（约 30 MB），`uptime.exe burn` / `uptime.exe eta` 可单开挂件
