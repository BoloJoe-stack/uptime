# uptime 运行时依赖（pip install -r requirements.txt）
rich>=13.7        # 终端渲染（tail 等模块）
keyboard          # 全局热键（focus / 老板键）
pywin32           # Windows 窗口枚举与前置（focus）
pystray           # 系统托盘（console）
Pillow            # 托盘图标 + burn 挂件像素金币渲染

# 打包 exe 才需要：py -3.10 build_exe.py → dist/uptime.exe
pyinstaller
